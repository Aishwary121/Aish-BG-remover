services:
  - type: web
    name: bg-remover-api
    env: python
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt && npm install"
    startCommand: "npm start"
    envVars:
      - key: FRONTEND_URL
        value: https://your-app.vercel.app
